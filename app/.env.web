# Activates the static web build (wasm worker backend). Loaded by Vite for
# `--mode web` (build:web / dev:web); absent for the desktop/fixture builds, so
# `caps.isWeb` stays false there and the web code is dead-code-eliminated.
VITE_BACKEND=web
